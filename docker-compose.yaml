version: "3.8"

services:
  linux-in-practice-2nd-container:
    build: .
    volumes: [.:/myapp]
    container_name: linux-in-practice-2nd-container
    image: linux-in-practice-2nd-image
    command: tail -F anything
